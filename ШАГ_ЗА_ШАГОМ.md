# 📱 ШАГ ЗА ШАГОМ - С КАРТИНКАМИ

## Пошаговая инструкция для копирования файлов

---

## 🎯 ШАГ 1: Откройте терминал в Figma Make

### Где найти терминал:

1. **Посмотрите внизу экрана Figma Make** - там должна быть панель "Terminal" или "Console"
2. Если не видите - нажмите на клавиатуре: **Ctrl + `** (обратная кавычка, рядом с 1)
3. Или найдите в меню: **View → Terminal**

Должно выглядеть примерно так:
```
Terminal
────────────────────────────────────────
$ _
```

---

## ✅ ШАГ 2: Вставьте команду

**Скопируйте эту команду полностью:**

```bash
cp -r . ~/Desktop/"стильный кабинет артиста 2"/
```

**Вставьте в терминал** (Cmd+V)

**Нажмите Enter**

---

## 🎉 ШАГ 3: Проверьте результат

Откройте Finder и перейдите в:
```
Desktop → стильный кабинет артиста 2
```

Вы должны увидеть ВСЕ файлы:
- ✅ src/
- ✅ supabase/
- ✅ deploy.sh
- ✅ test-api.html
- ✅ package.json
- ✅ и много других!

---

## 🚀 ШАГ 4: Git push

Откройте **обычный терминал** на Mac (не в Figma Make):

```bash
# Перейдите в папку
cd ~/Desktop/"стильный кабинет артиста 2"

# Push
git add .
git commit -m "feat: добавил CI/CD и тестирование"
git push
```

**ГОТОВО!** GitHub Actions автоматически все задеплоит! ✅

---

## 🆘 TROUBLESHOOTING

### Ошибка: "No such file or directory"

**Причина:** Папка не существует

**Решение:**
```bash
# Сначала создайте папку
mkdir -p ~/Desktop/"стильный кабинет артиста 2"

# Потом копируйте
cp -r . ~/Desktop/"стильный кабинет артиста 2"/
```

---

### Ошибка: "Permission denied"

**Решение:**
```bash
# Попробуйте без кавычек
cp -r . ~/Desktop/стильный\ кабинет\ артиста\ 2/
```

---

### Не вижу терминал в Figma Make

**Решение 1:**
- Нажмите **Ctrl + `** (обратная кавычка)

**Решение 2:**
- Menu → View → Terminal

**Решение 3:**
- Откройте обычный терминал Mac
- Найдите где Figma Make хранит проект
- Скопируйте оттуда

---

## 💡 АЛЬТЕРНАТИВА: Через обычный терминал Mac

Если не можете найти терминал в Figma Make:

```bash
# Откройте обычный терминал Mac (Spotlight → Terminal)

# Найдите проекты Figma Make
find ~ -name "promo-music" -type d 2>/dev/null

# Скопируйте из найденной папки
cp -r /путь/к/найденной/папке/* ~/Desktop/"стильный кабинет артиста 2"/
```

---

## ✅ ПРОВЕРКА: Все скопировалось?

```bash
# Перейдите в целевую папку
cd ~/Desktop/"стильный кабинет артиста 2"

# Посмотрите что внутри
ls -la

# Вы должны увидеть:
# - папки: src/, supabase/, database/, docs/
# - файлы: deploy.sh, test-api.html, package.json
# - и ещё 200+ файлов
```

Если видите все файлы - **ОТЛИЧНО!** ✅

---

## 🎯 РЕЗЮМЕ

1. **Терминал в Figma Make** → `cp -r . ~/Desktop/"стильный кабинет артиста 2"/`
2. **Проверьте Finder** → файлы скопировались?
3. **Обычный терминал** → `cd ~/Desktop/"стильный кабинет артиста 2" && git push`

**3 шага = ГОТОВО!** 🚀

---

## 📞 НУЖНА ПОМОЩЬ?

Скажите на каком шаге застряли:
1. Не могу найти терминал в Figma Make?
2. Команда не работает?
3. Файлы не копируются?
4. Git push не работает?

**Я помогу!** 😊

