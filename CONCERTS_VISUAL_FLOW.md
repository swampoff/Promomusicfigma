# 🎸 CONCERTS DATA FLOW - ВИЗУАЛЬНАЯ СХЕМА

## 🔄 ПОЛНЫЙ ПОТОК ДАННЫХ КОНЦЕРТОВ

```
┌───────────────────────────────────────────────────────────────────────────────┐
│                         BACKEND (Supabase)                                    │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  PostgreSQL Database: tour_dates                                        │ │
│  ├─────────────────────────────────────────────────────────────────────────┤ │
│  │  Columns:                                                               │ │
│  │  • id, artist_id, title, description                                    │ │
│  │  • venue_name, city, country, date, show_start                          │ │
│  │  • ticket_price_min, ticket_price_max, ticket_url                       │ │
│  │  • is_promoted ← KEY FIELD!                                             │ │
│  │  • promotion_expires_at ← Когда заканчивается промо                     │ │
│  │  • moderation_status (draft/pending/approved/rejected)                  │ │
│  │  • views, clicks ← Аналитика                                            │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  API Routes: /make-server-84730125/api/concerts/                        │ │
│  ├─────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                         │ │
│  │  🔒 Authenticated Endpoints:                                            │ │
│  │  ├─ GET    /tour-dates              ← Мои концерты                     │ │
│  │  ├─ POST   /tour-dates              ← Создать концерт                  │ │
│  │  ├─ PUT    /tour-dates/:id          ← Редактировать                    │ │
│  │  ├─ DELETE /tour-dates/:id          ← Удалить                          │ │
│  │  ├─ POST   /tour-dates/:id/submit   ← На модерацию                     │ │
│  │  └─ POST   /tour-dates/:id/promote  ← Оплатить промо (коины)           │ │
│  │                                                                         │ │
│  │  🌐 Public Endpoint (НУЖНО СОЗДАТЬ):                                    │ │
│  │  └─ GET    /promoted                ← Все promoted концерты            │ │
│  │      WHERE is_promoted = true                                          │ │
│  │        AND promotion_expires_at > NOW()                                │ │
│  │        AND moderation_status = 'approved'                              │ │
│  │                                                                         │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘
                                         │
                                         │ HTTPS Request
                                         ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                                      │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  📂 /utils/api/concerts.ts (НУЖНО СОЗДАТЬ)                              │ │
│  ├─────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                         │ │
│  │  export async function getPromotedConcerts() {                         │ │
│  │    try {                                                               │ │
│  │      const response = await fetch(                                     │ │
│  │        `/api/concerts/promoted`,                                       │ │
│  │        { headers: { Authorization: Bearer ${publicAnonKey} } }         │ │
│  │      );                                                                │ │
│  │      return response.json();                                           │ │
│  │    } catch (error) {                                                   │ │
│  │      return DEMO_CONCERTS; ← Fallback!                                 │ │
│  │    }                                                                   │ │
│  │  }                                                                     │ │
│  │                                                                         │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                         │                                     │
│                                         │ import & useEffect                  │
│                                         ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  📂 /src/app/ArtistApp.tsx                                              │ │
│  ├─────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                         │ │
│  │  const [promotedConcerts, setPromotedConcerts] = useState([]);         │ │
│  │                                                                         │ │
│  │  useEffect(() => {                                                     │ │
│  │    getPromotedConcerts().then(setPromotedConcerts);                    │ │
│  │  }, []);                                                               │ │
│  │                                                                         │ │
│  │  // СЕЙЧАС: Hardcoded данные                                           │ │
│  │  const promotedConcerts = [                                            │ │
│  │    { id: 1, title: 'Summer Music Fest', ... },                         │ │
│  │    { id: 2, title: 'Акустический вечер', ... },                        │ │
│  │    { id: 3, title: 'Electronic Paradise', ... },                       │ │
│  │  ];                                                                    │ │
│  │                                                                         │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                         │                                     │
│                                         │ Props передача                      │
│                                         ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  📂 /src/app/components/home-page.tsx                                   │ │
│  ├─────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                         │ │
│  │  interface HomePageProps {                                             │ │
│  │    promotedConcerts?: PromotedConcert[];                               │ │
│  │    onNavigate: (section: string) => void;                              │ │
│  │  }                                                                     │ │
│  │                                                                         │ │
│  │  export function HomePage({ promotedConcerts, ... }) {                 │ │
│  │    console.log('🎸 HomePage promotedConcerts:', promotedConcerts);     │ │
│  │    ...                                                                 │ │
│  │  }                                                                     │ │
│  │                                                                         │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                          │                          │                         │
│                          ▼                          ▼                         │
│  ┌───────────────────────────────┐  ┌────────────────────────────────────┐   │
│  │  GRID СЕКЦИЯ (строка 407)    │  │  SIDEBAR (строка 503)              │   │
│  ├───────────────────────────────┤  ├────────────────────────────────────┤   │
│  │                               │  │                                    │   │
│  │  {promotedConcerts &&         │  │  {promotedConcerts &&              │   │
│  │   promotedConcerts.length > 0 │  │   promotedConcerts.length > 0 && ( │   │
│  │   && (                        │  │   <PromotedConcertsSidebar         │   │
│  │    <motion.div>               │  │     concerts={promotedConcerts}    │   │
│  │      <div className="grid     │  │   />                               │   │
│  │        grid-cols-3">          │  │  )}                                │   │
│  │        {promotedConcerts      │  │                                    │   │
│  │          .slice(0, 3)         │  │  ┌──────────────────────────────┐ │   │
│  │          .map(...)}           │  │  │ Carousel Component:          │ │   │
│  │      </div>                   │  │  │ • Auto-rotation (5 sec)      │ │   │
│  │    </motion.div>              │  │  │ • Manual navigation          │ │   │
│  │   )}                          │  │  │ • Pause on hover             │ │   │
│  │                               │  │  │ • Dot indicators             │ │   │
│  │  ┌─────────────────────────┐ │  │  └──────────────────────────────┘ │   │
│  │  │ Карточка концерта:      │ │  │                                    │   │
│  │  │ • Banner image          │ │  │                                    │   │
│  │  │ • Date badge (top-r)    │ │  │                                    │   │
│  │  │ • Title + artist        │ │  │                                    │   │
│  │  │ • Location + time       │ │  │                                    │   │
│  │  │ • Stats (views, clicks) │ │  │                                    │   │
│  │  └─────────────────────────┘ │  │                                    │   │
│  │                               │  │                                    │   │
│  └───────────────────────────────┘  └────────────────────────────────────┘   │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  📂 /src/app/components/landing/SunoLayoutLanding.tsx                   │ │
│  ├─────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                         │ │
│  │  // СЕЙЧАС: Hardcoded данные                                           │ │
│  │  const upcomingConcerts = [                                            │ │
│  │    { id: '1', title: 'Summer Music Fest', ... },                       │ │
│  │    { id: '2', title: 'Акустический вечер', ... },                      │ │
│  │    { id: '3', title: 'Electronic Paradise', ... },                     │ │
│  │    { id: '4', title: 'Rock Night', ... },                              │ │
│  │  ];                                                                    │ │
│  │                                                                         │ │
│  │  // НУЖНО: Load from API                                               │ │
│  │  useEffect(() => {                                                     │ │
│  │    getPromotedConcerts().then(setUpcomingConcerts);                    │ │
│  │  }, []);                                                               │ │
│  │                                                                         │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                         │                                     │
│                                         ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  HORIZONTAL CAROUSEL (строка 873)                                       │ │
│  ├─────────────────────────────────────────────────────────────────────────┤ │
│  │                                                                         │ │
│  │  <div className="flex gap-4 overflow-x-auto scrollbar-hide snap-x">    │ │
│  │    {upcomingConcerts.map((concert, index) => (                         │ │
│  │      <motion.div className="w-[320px] snap-start">                     │ │
│  │        {/* Вертикальная карточка */}                                    │ │
│  │        <div className="aspect-[4/5] rounded-2xl">                      │ │
│  │          <img src={concert.image} />                                   │ │
│  │          {/* Date Badge */}                                            │ │
│  │          {/* Views Badge */}                                           │ │
│  │          {/* Info Overlay */}                                          │ │
│  │          {/* Price Button */}                                          │ │
│  │        </div>                                                          │ │
│  │      </motion.div>                                                     │ │
│  │    ))}                                                                 │ │
│  │  </div>                                                                │ │
│  │                                                                         │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 КЛЮЧЕВЫЕ ПОЛЯ ДАННЫХ

```typescript
┌────────────────────────────────────────────────────────────────┐
│  PromotedConcert Interface                                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  id: number                    ← Уникальный ID                │
│  title: string                 ← "Summer Music Fest 2026"     │
│  date: string                  ← "2026-07-15" (ISO format)    │
│  time: string                  ← "18:00"                      │
│  city: string                  ← "Москва"                     │
│  venue: string                 ← "Olympic Stadium"            │
│  type: string                  ← "Фестиваль", "DJ сет", etc.  │
│  description: string           ← Полное описание              │
│  banner: string                ← URL картинки                 │
│  ticketPriceFrom: string       ← "2000"                       │
│  ticketPriceTo: string         ← "8000"                       │
│  ticketLink: string            ← URL для покупки              │
│  views: number                 ← 15400 (аналитика)            │
│  clicks: number                ← 850 (аналитика)              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 PROMOTION WORKFLOW

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      ЖИЗНЕННЫЙ ЦИКЛ КОНЦЕРТА                            │
└─────────────────────────────────────────────────────────────────────────┘

1️⃣  СОЗДАНИЕ
    ┌──────────────────────────────────────────┐
    │ POST /tour-dates                         │
    │ {                                        │
    │   title: "Summer Fest",                  │
    │   date: "2026-07-15",                    │
    │   city: "Москва",                        │
    │   ...                                    │
    │ }                                        │
    └──────────────────────────────────────────┘
                    │
                    ▼
    ┌──────────────────────────────────────────┐
    │ Database Record Created:                 │
    │ • status: 'draft'                        │
    │ • moderation_status: 'draft'             │
    │ • is_promoted: false                     │
    └──────────────────────────────────────────┘

2️⃣  МОДЕРАЦИЯ
    ┌──────────────────────────────────────────┐
    │ POST /tour-dates/:id/submit              │
    └──────────────────────────────────────────┘
                    │
                    ▼
    ┌──────────────────────────────────────────┐
    │ moderation_status: 'pending'             │
    └──────────────────────────────────────────┘
                    │
                    ▼
    ┌──────────────────────────────────────────┐
    │ Модератор проверяет                      │
    └──────────────────────────────────────────┘
                    │
                    ▼
    ┌──────────────────────────────────────────┐
    │ moderation_status: 'approved'            │
    │ status: 'published'                      │
    └──────────────────────────────────────────┘

3️⃣  ПРОМО (ОПЛАТА КОИНАМИ)
    ┌──────────────────────────────────────────┐
    │ POST /tour-dates/:id/promote             │
    │ {                                        │
    │   days: 7  // Длительность промо         │
    │ }                                        │
    └──────────────────────────────────────────┘
                    │
                    ▼
    ┌──────────────────────────────────────────┐
    │ Database Update:                         │
    │ • is_promoted: true ✅                   │
    │ • promotion_expires_at:                  │
    │     NOW() + 7 days                       │
    │                                          │
    │ User Balance:                            │
    │ • coins -= PROMOTION_COST                │
    └──────────────────────────────────────────┘

4️⃣  ПУБЛИКАЦИЯ НА ГЛАВНОЙ
    ┌──────────────────────────────────────────┐
    │ GET /promoted                            │
    │                                          │
    │ SELECT * FROM tour_dates                 │
    │ WHERE is_promoted = true                 │
    │   AND promotion_expires_at > NOW()       │
    │   AND moderation_status = 'approved'     │
    │   AND date >= CURRENT_DATE               │
    │ ORDER BY date ASC                        │
    │ LIMIT 10;                                │
    └──────────────────────────────────────────┘
                    │
                    ▼
    ┌──────────────────────────────────────────┐
    │ FRONTEND ОТОБРАЖЕНИЕ:                    │
    │ ✅ HomePage (grid + sidebar)             │
    │ ✅ SunoLayoutLanding (carousel)          │
    └──────────────────────────────────────────┘

5️⃣  ИСТЕЧЕНИЕ ПРОМО
    ┌──────────────────────────────────────────┐
    │ Автоматически через 7 дней:              │
    │ promotion_expires_at < NOW()             │
    │                                          │
    │ → Концерт НЕ показывается в /promoted    │
    │ → is_promoted остается true (история)    │
    └──────────────────────────────────────────┘
```

---

## 📊 КОМПОНЕНТЫ И ИХ РОЛИ

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          КОМПОНЕНТЫ СИСТЕМЫ                             │
└─────────────────────────────────────────────────────────────────────────┘

📂 /src/app/components/promoted-concerts-sidebar.tsx
├─ PromotedConcert (interface)          ← Типизация данных
├─ PromotedConcertsSidebar (component)  ← Карусель для sidebar
│  ├─ State: currentIndex               ← Текущий слайд
│  ├─ State: isAutoPlaying              ← Auto-rotation вкл/выкл
│  ├─ Auto-rotation timer (5 sec)       ← useEffect
│  └─ Navigation: prev/next/dots        ← Кнопки + индикаторы

📂 /src/app/components/home-page.tsx
├─ HomePage (component)
│  ├─ Props: promotedConcerts[]         ← Получает данные от родителя
│  ├─ Grid Section (строка 407)         ← 3 карточки
│  │  └─ .slice(0, 3)                   ← Только первые 3
│  └─ Sidebar (строка 503)              ← Полная карусель
│     └─ <PromotedConcertsSidebar />

📂 /src/app/components/landing/SunoLayoutLanding.tsx
├─ SunoLayoutLanding (component)
│  ├─ Local state: upcomingConcerts[]   ← СЕЙЧАС: hardcoded
│  └─ Carousel (строка 873)             ← Горизонтальный scroll
│     ├─ Вертикальные карточки          ← aspect-[4/5]
│     ├─ Snap scroll                    ← snap-x
│     └─ 4 концерта                     ← .map()

📂 /src/app/ArtistApp.tsx
├─ ArtistApp (component)
│  ├─ State: promotedConcerts[]         ← СЕЙЧАС: hardcoded
│  │                                    ← НУЖНО: useEffect + API
│  └─ renderContent()
│     └─ case 'home': <HomePage promotedConcerts={...} />

📂 /supabase/functions/server/concerts-routes.tsx
├─ GET    /tour-dates                   ← Мои концерты (auth)
├─ POST   /tour-dates                   ← Создать (auth)
├─ PUT    /tour-dates/:id               ← Редактировать (auth)
├─ DELETE /tour-dates/:id               ← Удалить (auth)
├─ POST   /tour-dates/:id/submit        ← На модерацию (auth)
├─ POST   /tour-dates/:id/promote       ← Оплатить промо (auth)
└─ GET    /promoted                     ← ❌ НУЖНО СОЗДАТЬ (public)
```

---

## ⚡ ТОЧКИ ИНТЕГРАЦИИ

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     ГДЕ НУЖНЫ ИЗМЕНЕНИЯ                                 │
└─────────────────────────────────────────────────────────────────────────┘

🔴 КРИТИЧЕСКИЕ (без них не работает):
├─ 1. Создать /promoted endpoint в concerts-routes.tsx
│     └─ Возвращает: WHERE is_promoted = true AND expires > NOW()
│
├─ 2. Создать /utils/api/concerts.ts
│     ├─ getPromotedConcerts()
│     └─ Fallback на DEMO_CONCERTS

🟡 ВАЖНЫЕ (для полной функциональности):
├─ 3. Обновить ArtistApp.tsx
│     ├─ useEffect(() => loadConcerts())
│     └─ useState для promotedConcerts
│
├─ 4. Обновить SunoLayoutLanding.tsx
│     ├─ useEffect(() => loadConcerts())
│     └─ useState для upcomingConcerts

🟢 ДОПОЛНИТЕЛЬНЫЕ (улучшения):
├─ 5. Добавить loading states
├─ 6. Добавить error handling
├─ 7. Добавить кэширование (React Query)
├─ 8. Добавить аналитику (track views/clicks)
└─ 9. Добавить фильтры (город, жанр)
```

---

## 🎨 ДИЗАЙН КАРТОЧЕК

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      АНАТОМИЯ КАРТОЧКИ КОНЦЕРТА                         │
└─────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────┐
│ 👁️ 15.4K        [15 ИЮЛ]             │ ← Badges (views + date)
│                                       │
│                                       │
│         [Banner Image]                │ ← Image с hover scale
│                                       │
│                                       │
│ ┌───────────────────────────────────┐ │
│ │ Summer Music Fest 2026            │ │ ← Title (bold)
│ │ Разные исполнители                │ │ ← Artist (light)
│ │                                   │ │
│ │ 📍 Москва • Olympic Stadium       │ │ ← Location
│ │ 🎵 18:00                          │ │ ← Time
│ │                                   │ │
│ │ ┌─────────────────────────────┐   │ │
│ │ │ от 2000 ₽              →   │   │ │ ← Price + Arrow
│ │ └─────────────────────────────┘   │ │
│ └───────────────────────────────────┘ │
└───────────────────────────────────────┘

СТИЛИ:
• Background: gradient-to-br from-[#FF577F]/20 to-[#3E4C5E]/20
• Border: border-white/10 → hover:border-purple-400/50
• Shadow: hover:shadow-lg
• Hover: scale-110 (image), translate-x (arrow)
```

---

## 📈 АНАЛИТИКА

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        TRACKING EVENTS                                  │
└─────────────────────────────────────────────────────────────────────────┘

📊 Views (Просмотры):
├─ Когда: Карточка появляется в viewport
├─ Increment: tour_dates.views += 1
└─ API: POST /tour-dates/:id/track-view

👆 Clicks (Клики):
├─ Когда: Клик на карточку или "Купить билет"
├─ Increment: tour_dates.clicks += 1
└─ API: POST /tour-dates/:id/track-click

💰 Conversions (Конверсии):
├─ Когда: Переход по ticketLink
├─ Track: External link click
└─ API: POST /analytics/concert-conversion
```

---

## 🚀 ROADMAP ИНТЕГРАЦИИ

```
PHASE 1: Backend Setup
└─ [ ] Создать GET /promoted endpoint
   └─ [ ] Тестировать с Postman
   
PHASE 2: Frontend API
└─ [ ] Создать /utils/api/concerts.ts
   ├─ [ ] getPromotedConcerts()
   ├─ [ ] getUserConcerts()
   └─ [ ] DEMO_CONCERTS fallback

PHASE 3: Component Integration
├─ [ ] ArtistApp.tsx + useEffect
├─ [ ] SunoLayoutLanding.tsx + useEffect
└─ [ ] Loading/Error states

PHASE 4: Analytics
├─ [ ] Track views
├─ [ ] Track clicks
└─ [ ] Dashboard metrics

PHASE 5: Optimization
├─ [ ] React Query caching
├─ [ ] Infinite scroll
├─ [ ] Фильтры/поиск
└─ [ ] Server-side pagination
```
