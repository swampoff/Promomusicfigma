# 📁 КАК ПЕРЕНЕСТИ ВСЕ ФАЙЛЫ НА РАБОЧИЙ СТОЛ

## Простая инструкция для переноса в папку "стильный кабинет артиста 2"

---

## 🎯 ЧТО ВАМ НУЖНО

Перенести все созданные файлы из проекта Figma Make в:
```
~/Desktop/стильный кабинет артиста 2/
```

---

## ⚡ САМЫЙ ПРОСТОЙ СПОСОБ

### ВАРИАНТ 1: Автоматический скрипт (1 команда!)

```bash
# Запустите этот скрипт в терминале:
chmod +x copy-to-desktop.sh && ./copy-to-desktop.sh
```

**Готово!** Скрипт:
- ✅ Создаст папку если не существует
- ✅ Скопирует ВСЕ файлы (CI/CD, тесты, документация, backend, frontend)
- ✅ Сделает .sh файлы исполняемыми
- ✅ Покажет детальный отчет

---

### ВАРИАНТ 2: Ручное копирование (2 команды)

```bash
# 1. Создайте папку
mkdir -p ~/Desktop/"стильный кабинет артиста 2"

# 2. Скопируйте все
cp -r . ~/Desktop/"стильный кабинет артиста 2"/
```

---

## 📍 ГДЕ СЕЙЧАС НАХОДЯТСЯ ФАЙЛЫ?

Узнайте текущее расположение проекта Figma Make:

```bash
pwd
```

Скорее всего это что-то типа:
```
/Users/ваше-имя/Documents/Figma Make/promo-music
```

или
```
/var/folders/.../figma-make-project
```

---

## 🔄 ПОСЛЕ КОПИРОВАНИЯ

### Шаг 1: Перейдите в целевую папку

```bash
cd ~/Desktop/"стильный кабинет артиста 2"
```

### Шаг 2: Проверьте что все скопировалось

```bash
ls -la
```

Вы должны увидеть:
```
.github/
supabase/
src/
deploy.sh
test-api.html
test-api.mjs
vercel.json
package.json
*.md (много документации)
... и другие файлы
```

### Шаг 3: Инициализируйте git (если еще не сделано)

```bash
# Проверьте есть ли уже git
git status

# Если нет, инициализируйте:
git init
git remote add origin https://github.com/ваш-username/ваш-repo.git
```

### Шаг 4: Коммит и push

```bash
# Добавьте все файлы
git add .

# Коммит
git commit -m "feat: добавил полную систему CI/CD, тестирование и автоматизацию

- GitHub Actions для auto-deploy
- Deployment скрипты
- API тестеры (browser + CLI)
- Zod валидация
- Полная документация"

# Push
git push -u origin main
```

---

## 🎯 РЕКОМЕНДУЕМЫЙ WORKFLOW

### Если вы работаете в Figma Make:

```bash
# После изменений в Figma Make:
./copy-to-desktop.sh

# Затем в целевой папке:
cd ~/Desktop/"стильный кабинет артиста 2"
git add .
git commit -m "Update"
git push
```

### Если вы работаете напрямую в целевой папке:

```bash
# Просто работайте здесь:
cd ~/Desktop/"стильный кабинет артиста 2"

# И коммитьте как обычно:
git add .
git commit -m "Мои изменения"
git push
```

---

## 📋 ЧТО БУДЕТ СКОПИРОВАНО

### CI/CD (3 файла):
- `.github/workflows/deploy.yml` - GitHub Actions
- `deploy.sh` - deployment скрипт
- `vercel.json` - конфигурация Vercel

### Testing (7 файлов):
- `test-api.html` - браузерный тестер
- `test-api.mjs` - CLI тестер
- `API_TESTING_GUIDE.md` - полный гайд по API
- `HOW_TO_TEST_API.md` - инструкции
- `QUICK_TEST_COMMANDS.md` - команды curl
- `API_TESTING_READY.md` - статус
- Другие MD файлы

### Backend:
- `supabase/` - вся папка с Edge Functions

### Frontend:
- `src/` - вся папка с компонентами

### Конфигурация:
- `package.json`
- `tsconfig.json`
- `vite.config.ts`
- `.gitignore`

---

## 🐛 TROUBLESHOOTING

### ❌ Проблема: "Permission denied"

```bash
chmod +x copy-to-desktop.sh
```

### ❌ Проблема: "No such file or directory"

```bash
# Убедитесь что вы в правильной папке:
pwd

# Должно показать папку проекта Figma Make
```

### ❌ Проблема: Папка с пробелами не работает

```bash
# Используйте кавычки:
cd ~/Desktop/"стильный кабинет артиста 2"

# Или экранируйте пробелы:
cd ~/Desktop/стильный\ кабинет\ артиста\ 2
```

---

## 💡 АЛЬТЕРНАТИВА: Работайте прямо в нужной папке

Вместо копирования, можно открыть папку "стильный кабинет артиста 2" в Figma Make:

1. Откройте Figma Make
2. File → Open Folder
3. Выберите `~/Desktop/стильный кабинет артиста 2`
4. Все изменения будут сразу в нужной папке!

---

## ✅ БЫСТРЫЙ ЧЕКЛИСТ

Выполните по порядку:

- [ ] Запустите `./copy-to-desktop.sh`
- [ ] Проверьте что файлы скопировались (`ls -la`)
- [ ] Перейдите в целевую папку (`cd ~/Desktop/"стильный кабинет артиста 2"`)
- [ ] Инициализируйте git если нужно (`git init`)
- [ ] Добавьте remote (`git remote add origin ...`)
- [ ] Коммит (`git add . && git commit -m "..."`)
- [ ] Push (`git push -u origin main`)

---

## 🎉 ГОТОВО!

После этого все файлы будут в нужной папке и готовы к работе!

**Рекомендую:** Откройте папку "стильный кабинет артиста 2" прямо в Figma Make чтобы не копировать каждый раз.

---

**Следующий шаг:** Запустите `./copy-to-desktop.sh` прямо сейчас! 🚀

