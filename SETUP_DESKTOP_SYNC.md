# 📁 НАСТРОЙКА СИНХРОНИЗАЦИИ С РАБОЧИМ СТОЛОМ

## Как перенести все файлы в папку "стильный кабинет артиста 2"

---

## 🎯 ПРОБЛЕМА

Figma Make работает в своей папке, а вам нужно чтобы все файлы были в:
```
~/Desktop/стильный кабинет артиста 2/
```

---

## ✅ РЕШЕНИЕ 1: Автоматический скрипт (рекомендуется)

### Шаг 1: Сделайте скрипт исполняемым

```bash
chmod +x sync-to-desktop.sh
```

### Шаг 2: Запустите синхронизацию

```bash
./sync-to-desktop.sh
```

**Что он делает:**
- ✅ Создает папку если не существует
- ✅ Копирует ВСЕ созданные файлы
- ✅ Сохраняет структуру папок
- ✅ Делает .sh файлы исполняемыми
- ✅ Показывает что скопировано

---

## ✅ РЕШЕНИЕ 2: Ручное копирование

```bash
# 1. Создайте папку если не существует
mkdir -p ~/Desktop/"стильный кабинет артиста 2"

# 2. Узнайте где находится проект Figma Make
pwd

# 3. Скопируйте все файлы
cp -r . ~/Desktop/"стильный кабинет артиста 2"/

# 4. Перейдите в целевую папку
cd ~/Desktop/"стильный кабинет артиста 2"

# 5. Проверьте
ls -la
```

---

## ✅ РЕШЕНИЕ 3: Работать прямо в нужной папке

### Вариант A: Переместить проект Figma Make

```bash
# 1. Закройте Figma Make
# 2. Переместите папку проекта
mv /текущая/папка/проекта ~/Desktop/"стильный кабинет артиста 2"

# 3. Откройте проект в Figma Make из новой папки
```

### Вариант B: Создать симлинк

```bash
# Создать символическую ссылку
ln -s ~/Desktop/"стильный кабинет артиста 2" /путь/к/figma-make/проекту
```

---

## 🔄 АВТОМАТИЧЕСКАЯ СИНХРОНИЗАЦИЯ

Если вы хотите чтобы файлы автоматически копировались после каждого изменения:

### Создайте git hook

```bash
cd ~/Desktop/"стильный кабинет артиста 2"

# Инициализируйте git если еще не сделано
git init

# Создайте post-commit hook
cat > .git/hooks/post-commit << 'EOF'
#!/bin/bash
echo "🔄 Синхронизация файлов..."
rsync -av --exclude '.git' . ~/Desktop/"стильный кабинет артиста 2"/
echo "✅ Синхронизация завершена!"
EOF

chmod +x .git/hooks/post-commit
```

---

## 📋 СПИСОК ФАЙЛОВ ДЛЯ КОПИРОВАНИЯ

Вот все файлы которые я создал:

### CI/CD (3 файла):
```
.github/workflows/deploy.yml
deploy.sh
vercel.json
```

### Testing (7 файлов):
```
test-api.html
test-api.mjs
API_TESTING_GUIDE.md
HOW_TO_TEST_API.md
QUICK_TEST_COMMANDS.md
API_TESTING_READY.md
TESTING_COMPLETE_SUMMARY.md
README_TESTING.md
```

### Deployment (3 файла):
```
DEPLOYMENT_SETUP.md
QUICK_DEPLOY_GUIDE.md
START_HERE_DEPLOYMENT.md
```

### Backend (1 файл):
```
supabase/functions/server/validation-schemas.tsx
```

### Utils (2 файла):
```
sync-to-desktop.sh
SETUP_DESKTOP_SYNC.md
```

**Всего: ~18 файлов**

---

## 🎯 РЕКОМЕНДУЕМЫЙ WORKFLOW

### 1. Первая настройка:

```bash
# A. Запустите синхронизацию
chmod +x sync-to-desktop.sh
./sync-to-desktop.sh

# B. Перейдите в целевую папку
cd ~/Desktop/"стильный кабинет артиста 2"

# C. Инициализируйте git если нужно
git init
git remote add origin https://github.com/your-username/your-repo.git

# D. Коммит и push
git add .
git commit -m "feat: добавил CI/CD и тестирование"
git push -u origin main
```

---

### 2. Ежедневная работа:

**Вариант A: Работаете в Figma Make**
```bash
# После изменений запустите:
./sync-to-desktop.sh

# Затем в целевой папке:
cd ~/Desktop/"стильный кабинет артиста 2"
git add .
git commit -m "Update"
git push
```

**Вариант B: Работаете в целевой папке**
```bash
# Просто работайте напрямую в:
cd ~/Desktop/"стильный кабинет артиста 2"

# И делайте коммиты как обычно:
git add .
git commit -m "Update"
git push
```

---

## 🐛 TROUBLESHOOTING

### Проблема: Permission denied

```bash
# Сделайте скрипт исполняемым
chmod +x sync-to-desktop.sh
```

### Проблема: Папка не существует

```bash
# Создайте вручную
mkdir -p ~/Desktop/"стильный кабинет артиста 2"
```

### Проблема: Файлы не копируются

```bash
# Проверьте текущую директорию
pwd

# Проверьте что файлы существуют
ls -la

# Запустите с подробным выводом
bash -x sync-to-desktop.sh
```

---

## ✅ БЫСТРЫЙ СТАРТ

```bash
# 1. Одна команда для всего:
chmod +x sync-to-desktop.sh && ./sync-to-desktop.sh

# 2. Перейдите в целевую папку:
cd ~/Desktop/"стильный кабинет артиста 2"

# 3. Проверьте что все скопировано:
ls -la

# 4. Готово!
```

---

## 💡 АЛЬТЕРНАТИВА: Переместите весь проект

Самый простой способ:

```bash
# 1. Узнайте где проект Figma Make
pwd

# 2. Скопируйте ВСЮ папку
cp -r . ~/Desktop/"стильный кабинет артиста 2"/

# 3. Работайте в новой папке
cd ~/Desktop/"стильный кабинет артиста 2"

# 4. Откройте эту папку в Figma Make
```

---

**Выберите удобный вам способ и начинайте! 🚀**

