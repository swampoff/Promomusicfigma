# 🚀 Deployment Checklist для Supabase

## Проблема: Изменения не отображаются после деплоя

Если вы запушили код на GitHub и задеплоили на Supabase, но изменения не отображаются, выполните следующие шаги:

---

## ✅ Чеклист деплоя

### 1. **Проверка Git коммита**
```bash
# Убедитесь, что все изменения закоммичены
git status

# Проверьте последний коммит
git log -1

# Убедитесь, что запушили в правильную ветку
git push origin main
```

### 2. **Очистка кэша браузера**
- **Chrome/Edge**: `Ctrl + Shift + Delete` → Очистить кэш и изображения
- **Firefox**: `Ctrl + Shift + Delete` → Кэш
- **Safari**: `Cmd + Option + E`

**ИЛИ** Hard Refresh:
- **Windows**: `Ctrl + F5` или `Ctrl + Shift + R`
- **Mac**: `Cmd + Shift + R`

### 3. **Проверка Supabase Edge Functions**
Если вы используете Edge Functions, они могут кэшироваться:

```bash
# Перезапустите Edge Functions через Supabase CLI
supabase functions deploy

# Или через веб-интерфейс Supabase:
# Project → Edge Functions → Restart
```

### 4. **Очистка Vite кэша**
```bash
# Удалите папки кэша
rm -rf node_modules/.vite
rm -rf dist

# Пересоберите проект
npm run build
# или
pnpm build
```

### 5. **Проверка деплоя на Supabase**
1. Откройте **Supabase Dashboard**
2. Перейдите в **Settings → General**
3. Проверьте дату последнего деплоя
4. Если дата не обновилась — **Redeploy** вручную

### 6. **Проверка переменных окружения**
Убедитесь, что на Supabase настроены все необходимые переменные:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

### 7. **Открыть в режиме инкогнито**
- **Chrome**: `Ctrl + Shift + N`
- **Firefox**: `Ctrl + Shift + P`
- **Safari**: `Cmd + Shift + N`

Это позволит исключить проблемы с локальным кэшем.

---

## 🔧 Команды для деплоя

### Стандартный деплой:
```bash
# 1. Коммит изменений
git add .
git commit -m "feat: добавлен раздел новостей"
git push origin main

# 2. Пересборка (если требуется)
npm run build

# 3. Деплой через Supabase CLI
supabase db push
supabase functions deploy
```

### Принудительная перезагрузка:
```bash
# Очистка всех кэшей
rm -rf node_modules/.vite dist .next

# Переустановка зависимостей
npm install

# Сборка
npm run build
```

---

## 🐛 Диагностика проблем

### Проверка 1: Файл существует в репозитории
```bash
# Проверьте наличие файла в Git
git ls-files | grep NewsSection.tsx
```

### Проверка 2: Импорты корректны
Откройте `/src/app/components/landing/SunoLayoutLanding.tsx` и убедитесь:
```typescript
import { NewsSection } from './NewsSection';
```

### Проверка 3: Нет ошибок в консоли браузера
1. Откройте DevTools (`F12`)
2. Перейдите в **Console**
3. Проверьте на наличие ошибок (красный текст)

### Проверка 4: Network Tab
1. Откройте DevTools (`F12`)
2. Вкладка **Network**
3. Обновите страницу (`Ctrl + R`)
4. Проверьте, загружаются ли файлы JS/CSS

---

## 🚨 Частые проблемы

### 1. **Кэш CDN/Edge Network**
Если используется CDN (Cloudflare, Vercel Edge), кэш может сохраняться до 24 часов.

**Решение**: Добавить query параметр:
```
https://your-site.com/?v=2
```

### 2. **Service Worker кэш**
Если в приложении есть Service Worker, он может кэшировать старую версию.

**Решение**:
```javascript
// Очистить Service Worker
navigator.serviceWorker.getRegistrations().then(registrations => {
  registrations.forEach(registration => registration.unregister());
});
```

### 3. **Build не включает новые файлы**
**Решение**:
```bash
# Удалите dist и пересоберите
rm -rf dist
npm run build
```

---

## ✅ Финальная проверка

После выполнения всех шагов:

1. ✅ Открыть сайт в **режиме инкогнито**
2. ✅ Проверить **DevTools Console** на ошибки
3. ✅ Убедиться, что дата деплоя на Supabase обновилась
4. ✅ Проверить раздел "Новости" доступен в навигации

---

## 📞 Поддержка

Если проблема сохраняется после всех шагов:
1. Проверьте логи Supabase: **Project → Logs**
2. Проверьте статус билда: **Project → Edge Functions**
3. Откройте issue в репозитории с деталями

---

**Последнее обновление**: 7 февраля 2026
