# ✅ Проверка деплоя - Promo.Music

## 🎯 Текущий статус компонентов

### ✅ NewsSection.tsx
- **Файл**: `/src/app/components/landing/NewsSection.tsx`
- **Статус**: ✅ Существует и правильно экспортирован
- **Экспорт**: `export function NewsSection()`
- **Размер**: ~900 строк кода
- **Содержание**: 30 реальных новостей с полным адаптивом

### ✅ SunoLayoutLanding.tsx
- **Импорт**: ✅ `import { NewsSection } from './NewsSection'`
- **Использование**: ✅ `{activeNav === 'news' && <NewsSection />}`
- **Навигация**: ✅ Кнопка "Новости" в меню

### ✅ Структура файлов
```
src/
└── app/
    └── components/
        └── landing/
            ├── NewsSection.tsx          ✅ ГОТОВ
            ├── ChartsSection.tsx         ✅ ГОТОВ
            └── SunoLayoutLanding.tsx     ✅ ГОТОВ
```

---

## 🚨 Почему изменения не видны?

### 1. **Кэш браузера** (90% случаев)
Браузер кэширует JS/CSS файлы.

**Решение**:
- Hard Refresh: `Ctrl + Shift + R` (Windows) или `Cmd + Shift + R` (Mac)
- Или откройте в **режиме инкогнито**: `Ctrl + Shift + N`

### 2. **Кэш Supabase Edge**
Supabase кэширует статические файлы.

**Решение**:
1. Откройте **Supabase Dashboard**
2. Перейдите в **Project Settings**
3. Нажмите **Restart Project**

### 3. **Build не выполнен**
После git push нужно выполнить build.

**Решение**:
```bash
npm run build
# или
pnpm build
```

### 4. **Vite dev cache**
Vite кэширует модули во время разработки.

**Решение**:
```bash
rm -rf node_modules/.vite
npm run dev
```

---

## 🔍 Как проверить, что всё работает

### Шаг 1: Проверка локально
```bash
# Запустите локальный dev сервер
npm run dev

# Откройте http://localhost:5173
# Нажмите на кнопку "Новости" в меню
# Должен открыться раздел с 30 новостями
```

### Шаг 2: Проверка в браузере
1. Откройте сайт в **режиме инкогнито**
2. Откройте **DevTools** (F12)
3. Перейдите на вкладку **Console**
4. Обновите страницу
5. Проверьте на ошибки

### Шаг 3: Проверка сети
1. В DevTools откройте вкладку **Network**
2. Обновите страницу
3. Найдите файл `NewsSection` в списке
4. Статус должен быть **200 OK**

### Шаг 4: Проверка Supabase
1. Откройте **Supabase Dashboard**
2. **Project → Logs**
3. Проверьте логи на ошибки
4. **Project → Edge Functions** → проверьте статус

---

## 📋 Чеклист деплоя

- [ ] Закоммичены все изменения (`git status` пустой)
- [ ] Запушены на GitHub (`git push origin main`)
- [ ] Выполнен build (`npm run build`)
- [ ] Перезапущен Supabase Project
- [ ] Очищен кэш браузера (Hard Refresh)
- [ ] Проверено в режиме инкогнито
- [ ] Проверены логи в Supabase
- [ ] Проверена консоль браузера (DevTools)

---

## 🐛 Диагностика

### Проверка 1: Файл в Git
```bash
git ls-files | grep NewsSection
# Должен вывести: src/app/components/landing/NewsSection.tsx
```

### Проверка 2: Последний коммит
```bash
git log -1 --oneline
# Должен показать ваш последний коммит
```

### Проверка 3: Импорт в коде
Найдите в `SunoLayoutLanding.tsx`:
```typescript
import { NewsSection } from './NewsSection';  // ✅ ПРАВИЛЬНО
```

### Проверка 4: Использование
Найдите в `SunoLayoutLanding.tsx`:
```typescript
{activeNav === 'news' && <NewsSection />}  // ✅ ПРАВИЛЬНО
```

---

## 💡 Быстрое решение

Если ничего не помогает:

```bash
# 1. Удалите все кэши
rm -rf node_modules/.vite dist .next

# 2. Переустановите зависимости
npm install

# 3. Пересоберите проект
npm run build

# 4. Закоммитьте и запушите
git add .
git commit -m "rebuild: force rebuild"
git push origin main

# 5. Откройте сайт в инкогнито
# Chrome: Ctrl + Shift + N
# Firefox: Ctrl + Shift + P
```

---

## ✅ Ожидаемый результат

После всех шагов вы должны увидеть:

1. **Кнопка "Новости"** в левой навигации (desktop) или в мобильном меню
2. При клике открывается **раздел новостей**
3. Отображается **30 реальных новостей** с:
   - Breaking News баннером
   - Статистикой (30 публикаций, просмотры, комментарии)
   - 8 категориями фильтрации
   - Адаптивной сеткой (1-5 колонок)
   - Авторами, тегами, геолокацией

---

## 📞 Помощь

Если проблема остаётся:
1. Сделайте скриншот **DevTools Console**
2. Проверьте **Network Tab** на 404 ошибки
3. Проверьте **Supabase Logs**
4. Опишите проблему с деталями

**Дата проверки**: 7 февраля 2026
