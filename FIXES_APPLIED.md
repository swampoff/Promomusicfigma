# ✅ Исправления ошибок Landing Page

## 🐛 Проблемы, которые были исправлены:

### 1. ❌ Multiple GoTrueClient instances
**Проблема:** Создавалось несколько экземпляров Supabase клиента
**Решение:** Переэкспорт существующего singleton вместо создания нового

**Файл:** `/src/lib/supabase-client.ts`
```typescript
// ❌ СТАРАЯ версия (создавала новый экземпляр)
let supabaseInstance: SupabaseClient | null = null;
export const supabase = createClient(...); // Создавал НОВЫЙ клиент!

// ✅ НОВАЯ версия (переэкспортирует существующий)
import { supabase, getSupabaseClient } from '@/utils/supabase/client';
export { supabase, getSupabaseClient }; // Использует СУЩЕСТВУЮЩИЙ singleton!
```

**Ключевое решение:**
- В проекте уже был правильный singleton в `/src/utils/supabase/client.ts`
- Мы переэкспортируем его вместо создания нового
- Теперь ОДИН экземпляр на всё приложение
- ✅ Нет Multiple GoTrueClient warning

---

### 2. ❌ Table 'radio_charts' not found
**Проблема:** Таблица `radio_charts` не существует в Supabase БД
**Решение:** Добавлен fallback на mock данные

**Изменено в файлах:**
- `/src/app/components/landing/TopChartsSection.tsx`
- `/src/app/components/landing/TrendingSection.tsx`

**Логика:**
```typescript
const { data, error } = await supabase.from('radio_charts').select('*');

if (error) {
  console.warn('Table not found, using mock data');
  setUseMockData(true);
  setTracks(mockChartData); // Fallback к mock данным
  return;
}

setUseMockData(false);
setTracks(data || []);
```

**Mock данные:** 8 треков с данными из популярных русских групп (Кино, ДДТ, Алиса и т.д.)

---

### 3. ❌ Column 'tracks.status' does not exist
**Проблема:** В таблице `tracks` нет колонки `status`
**Решение:** Убран фильтр `.eq('status', 'published')`

**Изменено в:** `/src/app/components/landing/NewReleasesSection.tsx`

**Старый запрос:**
```typescript
// ❌ Ошибка: колонка status не существует
const { data } = await supabase
  .from('tracks')
  .select('*')
  .eq('status', 'published') // ← эта строка убрана
  .order('created_at', { ascending: false })
  .limit(12);
```

**Новый запрос:**
```typescript
// ✅ Работает без фильтра по status
const { data } = await supabase
  .from('tracks')
  .select('*')
  .order('created_at', { ascending: false })
  .limit(12);
```

**Mock данные:** 6 треков с различными жанрами

---

## 📦 Новые файлы:

### 1. `/src/lib/supabase-client.ts`
Singleton экземпляр Supabase клиента для всего приложения

### 2. `/src/lib/index.ts`
Export файл для библиотеки

### 3. `/src/app/components/landing/DataSourceIndicator.tsx`
Компонент-индикатор для отображения информации о mock данных (опционально)

---

## 🔄 Обновленные файлы:

### 1. `/src/app/components/landing/TopChartsSection.tsx`
- ✅ Использует singleton Supabase клиент
- ✅ Fallback к mock данным
- ✅ Добавлены mock данные (8 треков)
- ✅ Graceful error handling

### 2. `/src/app/components/landing/NewReleasesSection.tsx`
- ✅ Использует singleton Supabase клиент
- ✅ Убран фильтр по `status`
- ✅ Fallback к mock данным
- ✅ Добавлены mock данные (6 треков)

### 3. `/src/app/components/landing/TrendingSection.tsx`
- ✅ Использует singleton Supabase клиент
- ✅ Fallback к mock данным
- ✅ Добавлены mock данные (8 треков)

---

## 🎯 Результат:

### Приложение теперь работает в двух режимах:

#### 1. **Production режим** (когда таблицы созданы в Supabase)
- Загружает реальные данные из БД
- Нет warnings в консоли
- Полная функциональность

#### 2. **Demo режим** (когда таблицы не созданы)
- Автоматически переключается на mock данные
- Показывает демо-контент
- Graceful degradation без ошибок
- Warning в консоли: "Table not found, using mock data"

---

## 🚀 Запуск SQL для создания таблиц:

Чтобы перейти в production режим, выполните SQL скрипт в Supabase SQL Editor:

### Файл: `/SUPABASE_SQL_SETUP.sql`

**Необходимые таблицы:**
1. `radio_charts` - для TOP 20 и Trending
2. `tracks` - для New Releases

**Команды:**
```sql
-- Выполните в Supabase SQL Editor
-- Скопируйте содержимое /SUPABASE_SQL_SETUP.sql
```

---

## ✅ Проверка исправлений:

### Console должна показывать:

**✅ БЕЗ ошибок:**
```
// Нет Multiple GoTrueClient warnings
// Нет PGRST205 errors
// Нет column does not exist errors
```

**⚠️ С предупреждениями (если таблицы не созданы):**
```javascript
console.warn('Table radio_charts not found, using mock data');
console.warn('Table tracks not ready, using mock data');
```

---

## 📊 Mock данные в деталях:

### TopChartsSection (8 треков):
- Кино - "Мой рок-н-ролл"
- Кино - "Группа крови"
- Кино - "Звезда по имени Солнце"
- Мумий Тролль - "Владивосток 2000"
- ДДТ - "Лето"
- Градусы - "Танцуй"
- Алиса - "Небо славян"
- Кипелов - "Я свободен"

### NewReleasesSection (6 треков):
- Indie Band - "Новый трек 2026"
- DJ Producer - "Электронная мечта"
- Summer Vibes - "Летний вайб"
- City Lights - "Городские огни"
- Midnight Drive - "Ночная дорога"
- Freedom Sounds - "Свобода"

### TrendingSection (8 треков):
- Trending Artist - "Viral Hit 2026" (95% trend score)
- Party Band - "Танцевальный хит" (88%)
- Rock Legends - "Рок-баллада" (82%)
- И другие...

---

## 🎨 UI/UX улучшения:

1. **Плавная загрузка:** Spinner анимация во время загрузки
2. **Error handling:** Нет красных ошибок, только warnings в console
3. **Graceful fallback:** Автоматический переход на mock данные
4. **Индикатор mock режима:** Можно добавить визуальный индикатор (компонент готов)

---

## 📝 Рекомендации:

### Для production deployment:
1. ✅ Выполните SQL скрипт `/SUPABASE_SQL_SETUP.sql`
2. ✅ Проверьте создание таблиц в Supabase Dashboard
3. ✅ Заполните таблицы данными (или используйте AI-агент парсинга)
4. ✅ Перезапустите приложение

### Для development:
- Mock данные позволяют работать без настройки БД
- Удобно для дизайна и тестирования UI
- Не требует доступа к Supabase

---

## 🔧 Отладка:

Если проблемы остались:

```typescript
// Проверьте singleton работает
import { supabase } from '@/lib/supabase-client';
console.log('Supabase client:', supabase);

// Проверьте подключение
const { data, error } = await supabase.from('radio_charts').select('count');
console.log('Connection test:', { data, error });
```

---

**Создано:** 6 февраля 2026  
**Версия исправлений:** 1.0.0  
**Статус:** ✅ Все ошибки исправлены